document. addEventListener("DOMContentLoaded", () => {
  document.getElementById("but").addEventListener("click", () => {
    
  });
});

function canestro() {
  let x = y = 0;


  do {
    if (y != -1) {
      y = 0;
    }

  } while (y >= 0);
}